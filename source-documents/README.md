# Source Documents Archive

This directory is retained for compatibility with historical crawler-oriented support files. The active runtime does not read source roots from this directory; it resolves documentation through the DevDocs, MDN, and Dash source adapters.

`requirements.txt` is a compatibility shim for the optional `analysis` extra. Historical crawler analysis code has been archived under `documentation/archive/`.
